import 'package:flutter/material.dart';

class ReviewBoostPostPage extends StatelessWidget {
  const ReviewBoostPostPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}