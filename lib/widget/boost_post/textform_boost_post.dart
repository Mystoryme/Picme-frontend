import 'package:flutter/material.dart';

class TextFormBoostPost extends StatelessWidget {
  const TextFormBoostPost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}